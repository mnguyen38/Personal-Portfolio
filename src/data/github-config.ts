// GitHub username to fetch repos from
export const GITHUB_USERNAME = "mnguyen38";

// Repos to mark as featured (use exact repo names)
// These will appear first and show the "Featured" badge
export const featuredRepos: string[] = [
  // Add your featured repo names here, e.g.:
  // "portfolio",
  // "awesome-project",
];

// Repos to exclude from the projects page (use exact repo names)
// Useful for hiding config repos, forks you don't want shown, etc.
export const excludedRepos: string[] = [
  // Add repo names to hide, e.g.:
  // "mnguyen38",  // GitHub profile README repo
  // ".github",
];
